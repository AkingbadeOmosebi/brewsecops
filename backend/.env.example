# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=akings_coffee       # Sample/Demo database name; should be changed in production.
DB_USER=postgres        # Demo user; should not be exposed in production.
DB_PASSWORD=your_password_here   # Demo password; should NEVER be exposed in production.

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:5173
