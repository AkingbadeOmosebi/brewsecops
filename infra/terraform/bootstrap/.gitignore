# Local .terraform directories (contains provider binaries)
**/.terraform/*

# .tfstate files (These contain secrets in plain text! DO NOT COMMIT)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude CLI configuration files
.terraformrc
terraform.rc

# EXCLUDE SECRET VARIABLE FILES (The most important part)
# You should keep your passwords/secrets in these files locally
*.tfvars
*.tfvars.json
*.env